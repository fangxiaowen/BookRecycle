/* This file lists requuirements we still need to satisfy in HW1. */

  
Use of HTML elements
	<table>, <form>, <link>, <input> (Xiaowen Fang will do this)

Use of CSS features
	At least 4 selector types
	At least 3 pseudo classes
	Padding, border
	Z-index & opacity
	At least 3 different positioning schemes
	Transform
	Transition

Support responsive layout on mobile

